# Optional contrast-experiment deps — SPEC §2.1
# Install only when running H5 / experiment path (P0 full acceptance).
# Install order (see README «P0 / 对照实验（H5）本地注意»):
#   1) torch matching your Python/OS (official matrix)
#   2) pip install -r requirements-experiment.txt
FlagEmbedding==1.3.5
modelscope==1.25.0
sentence_transformers==3.4.1
# Pin for GTE-Qwen2 remote code + DynamicCache API (4.57.x breaks encode)
transformers==4.46.3
